import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onsiteuitesting.demo',
  appName: 'OnSite UI Testing',
  webDir: '../../dist/apps/onsite/browser'
};

export default config;
